[project]
name = "datastats-gcp-jobs-scraper"
version = "1.0.0"
description = "Python script to retreive jobs information from urls list"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "loguru",
    "beautifulsoup4",
    "python-dotenv",
    "google-cloud-storage>=1.17.0",
    "google-cloud-bigquery",
    "google-cloud-logging",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.12.0",
]
